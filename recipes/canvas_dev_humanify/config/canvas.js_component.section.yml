langcode: en
status: true
dependencies: {}
machineName: section
name: Section
required: {}
props:
  darkVariant:
    title: "Dark variant"
    type: boolean
    examples:
      - false
      - false
  backgroundColor:
    title: "Background color"
    type: string
    examples:
      - mantle
      - crust
    enum:
      - base
      - mantle
      - crust
    "meta:enum":
      base: Base
      mantle: Mantle
      crust: Crust
slots:
  content:
    title: Content
js:
  original: |
    import { cva } from "class-variance-authority";
    import { cn } from "drupal-canvas";

    const Section = ({ content, darkVariant, backgroundColor }) => {
      const sectionVariants = cva("", {
        variants: {
          colorScheme: {
            light: "",
            dark: "dark",
          },
          backgroundColor: {
            base: "bg-base",
            mantle: "bg-mantle",
            crust: "bg-crust",
          },
        },
        defaultVariants: {
          colorScheme: "light",
        },
      });

      return (
        <section
          className={cn(
            sectionVariants({
              colorScheme: darkVariant ? "dark" : "light",
              backgroundColor,
            }),
          )}
        >
          <div className="mx-auto flex max-w-screen-xl min-w-sm flex-col items-center gap-6 p-12 px-4 md:p-16 md:px-12 lg:gap-8 lg:px-16">
            {content}
          </div>
        </section>
      );
    };

    export default Section;
  compiled: |
    import { jsx as _jsx } from "react/jsx-runtime";
    import { cva } from "class-variance-authority";
    import { cn } from "drupal-canvas";
    const Section = ({ content, darkVariant, backgroundColor })=>{
        const sectionVariants = cva("", {
            variants: {
                colorScheme: {
                    light: "",
                    dark: "dark"
                },
                backgroundColor: {
                    base: "bg-base",
                    mantle: "bg-mantle",
                    crust: "bg-crust"
                }
            },
            defaultVariants: {
                colorScheme: "light"
            }
        });
        return /*#__PURE__*/ _jsx("section", {
            className: cn(sectionVariants({
                colorScheme: darkVariant ? "dark" : "light",
                backgroundColor
            })),
            children: /*#__PURE__*/ _jsx("div", {
                className: "mx-auto flex max-w-screen-xl min-w-sm flex-col items-center gap-6 p-12 px-4 md:p-16 md:px-12 lg:gap-8 lg:px-16",
                children: content
            })
        });
    };
    export default Section;
css:
  original: ""
  compiled: ""
dataDependencies: {}
